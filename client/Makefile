buildrun:
	mkdir -p bin/
	gcc -g -lm -lncurses -Wall -o bin/client.out client.c
	./bin/client.out

build:
	mkdir -p bin/
	gcc -g -lm -lncurses -Wall -o bin/client.out client.c

run:
	./bin/client.out

clean:
	rm -r -f bin/

